apiVersion: v1
kind: Service

metadata:
  name: task-manager
  namespace: cloud-task-manager


spec:
  selector:
    app: task-manager

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP